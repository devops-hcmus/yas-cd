{{- if .Values.authorization.enabled }}
# ============================================================
# DENY-ALL Default Policy for namespace {{ .Release.Namespace }}
# ============================================================
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: deny-all-default
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "service-mesh.labels" . | nindent 4 }}
    purpose: security-baseline
spec:
  {}

---
# ============================================================
# ALLOW Policies for Backend Services
# ============================================================
{{- range .Values.backendServices }}
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: allow-{{ .name }}-access
  namespace: {{ $.Release.Namespace }}
  labels:
    {{- include "service-mesh.labels" $ | nindent 4 }}
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .name }}
  action: ALLOW
  rules:
    - from:
        # Nginx API Gateway (default SA)
        - source:
            principals:
              - "cluster.local/ns/{{ $.Release.Namespace }}/sa/default"    
        {{- range .allowedCallers }}
        # {{ . }}
        - source:
            principals:
              - "cluster.local/ns/{{ $.Release.Namespace }}/sa/{{ . }}"
        {{- end }}
      to:
        - operation:
            ports: ["80"]
    # Allow Prometheus metric scraping
    - from:
        - source:
            namespaces:
              - "{{ $.Values.observabilityNamespace }}"
      to:
        - operation:
            ports: ["{{ $.Values.metricPort }}"]
    # Allow Kubernetes health checks (kubelet probes on metric port)
    # This rule explicitly allows all traffic to metric port for health checks
    - to:
        - operation:
            ports: ["{{ $.Values.metricPort }}"]
---
{{- end }}

# ============================================================
# ALLOW Policies for BFF Services
# ============================================================
{{- range .Values.bffServices }}
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: allow-{{ . }}-access
  namespace: {{ $.Release.Namespace }}
  labels:
    {{- include "service-mesh.labels" $ | nindent 4 }}
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ . }}
  action: ALLOW
  rules:
    {{- if $.Values.allowNodePort }}
    # Allow all traffic (for NodePort/dev environment without mTLS identity)
    - to:
        - operation:
            ports: ["80"]
    {{- else }}
    - from:
        - source:
            principals:
              - "cluster.local/ns/{{ $.Release.Namespace }}/sa/default"
        - source:
            namespaces:
              - "{{ $.Values.ingressNamespace }}"
      to:
        - operation:
            ports: ["80"]
    {{- end }}
    # Allow Prometheus metric scraping
    - from:
        - source:
            namespaces:
              - "{{ $.Values.observabilityNamespace }}"
      to:
        - operation:
            ports: ["{{ $.Values.metricPort }}"]
    # Allow Kubernetes health checks (kubelet probes on metric port)
    # This rule explicitly allows all traffic to metric port for health checks
    - to:
        - operation:
            ports: ["{{ $.Values.metricPort }}"]
---
{{- end }}

# ============================================================
# ALLOW Policies for UI Services
# ============================================================
{{- range .Values.uiServices }}
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: allow-{{ .name }}-access
  namespace: {{ $.Release.Namespace }}
  labels:
    {{- include "service-mesh.labels" $ | nindent 4 }}
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .name }}
  action: ALLOW
  rules:
    {{- if $.Values.allowNodePort }}
    # Allow all traffic (for NodePort/dev environment without mTLS identity)
    - to:
        - operation:
            ports: ["{{ .port }}"]
    {{- else }}
    - from:
        - source:
            namespaces:
              - "{{ $.Release.Namespace }}"
              - "{{ $.Values.ingressNamespace }}"
      to:
        - operation:
            ports: ["{{ .port }}"]
    {{- end }}
---
{{- end }}

{{- if .Values.nginxGateway.enabled }}
# ============================================================
# ALLOW Policy for Nginx API Gateway
# ============================================================
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: allow-nginx-access
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "service-mesh.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{ .Values.nginxGateway.labelKey }}: {{ .Values.nginxGateway.labelValue }}
  action: ALLOW
  rules:
    {{- if .Values.allowNodePort }}
    # Allow all traffic (for NodePort/dev environment without mTLS identity)
    - to:
        - operation:
            ports: ["80"]
    {{- else }}
    - from:
        - source:
            namespaces:
              - "{{ .Release.Namespace }}"
              - "{{ .Values.ingressNamespace }}"
      to:
        - operation:
            ports: ["80"]
    {{- end }}
{{- end }}
{{- end }}